# roster-management-system
A role-based web application for managing employees, shifts, duty rosters, shift assignments, and shift-swap requests — built for **Vercel** (hosting) and **Neon Postgres** (database), at zero cost on their free tiers.
